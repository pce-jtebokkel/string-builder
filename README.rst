==============================
Codesys String Builder Library
==============================

The standard ``CONCAT`` function is really cumbersome to use to build complex
strings. This library implements a string builder method and some string format
functions.
